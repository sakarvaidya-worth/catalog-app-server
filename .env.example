# Firebase Admin SDK credentials will be loaded from Google Application Default Credentials
# Make sure your EC2 instance has the appropriate IAM role for Firebase Admin SDK

# AWS Configuration (automatically loaded from EC2 IAM role)
# AWS_REGION=us-east-1
S3_BUCKET_NAME=hetproductimages

# Server Configuration
PORT=5000